{
  "name": "example",
  "version": "1.0.0",
  "description": "Example router using @beaverfy/express-router",
  "main": "dist/index.js",
  "repository": "https://github.com/beaverfy/express-router/example",
  "author": "Turtlepaw",
  "license": "MIT",
  "private": false,
  "dependencies": {
    "express": "^4.18.2",
    "typescript": "^5.3.3"
  },
  "scripts": {
    "update": "cd .. && yarn prepare",
    "clean": "rm -rf ./dist",
    "build": "yarn clean && tsc",
    "start": "yarn build && node ./dist/index.js",
    "dev": "yarn update && yarn start"
  },
  "devDependencies": {
    "@types/express": "^4.17.21"
  }
}
